import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vrbe.nextsolutions.in',
  appName: ' Chat Applicatin P1',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
